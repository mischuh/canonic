"""``canon setup`` — project setup wizard (stub; E1)."""

import typer

from canon.cli.commands import not_implemented


def setup(ctx: typer.Context) -> None:
    """Run the interactive project setup wizard."""
    not_implemented(ctx, "setup")
