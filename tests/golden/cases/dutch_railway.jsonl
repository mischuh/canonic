{"id": "railway_service_count", "project": "dutch-railway", "why": "smoke test -- a total breakage here is one clean failure instead of many confusing ones", "query": {"metrics": ["service_count"]}}
{"id": "railway_service_count_vs_scheduled_stops", "project": "dutch-railway", "why": "781ae46: scope population_filter/guardrails per metric -- service_count carries the exclude-cancelled-arrivals guardrail, scheduled_stops does not, in the same multi-metric query; pre-fix the shared WHERE made both counts equal", "query": {"metrics": ["service_count", "scheduled_stops"]}}
{"id": "railway_service_count_vs_scheduled_stops_by_service_type", "project": "dutch-railway", "why": "781ae46 variant: same per-metric guardrail scoping, grouped by service_type", "query": {"metrics": ["service_count", "scheduled_stops"], "dimensions": ["service_type"]}}
{"id": "railway_cancelled_arrival_rate_by_province", "project": "dutch-railway", "why": "ratio metric replicates the full fact_services -> dim_nl_train_stations -> dim_nl_municipalities -> dim_nl_provinces join into both the numerator and denominator leaves -- the largest join graph in any example and the most likely to silently drift", "query": {"metrics": ["cancelled_arrival_rate"], "dimensions": ["province_name"]}}
{"id": "railway_service_count_and_cancelled_rate_by_province", "project": "dutch-railway", "why": "AMENDMENT-multi-metric-compose S11 AC2 + S12 AC2: a ratio and a scalar over the same fact table, where service_count's exclude-cancelled-arrivals guardrail gives it a different effective filter -- locks that leaf dedup never merges plans whose filters differ, the failure mode that would report one number for both columns", "query": {"metrics": ["service_count", "cancelled_arrival_rate"], "dimensions": ["province_name"]}}
