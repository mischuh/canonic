name: genres
connection: chinook_pg
table: 'chinook."Genre"'
grain: [GenreId]
description: "Music genre lookup. Join target for tracks."

columns:
  - { name: GenreId, type: int,    nullable: false }
  - { name: Name,    type: string, nullable: true  }

dimensions:
  - { name: genre_name, column: Name }

meta:
  provenance: human_curated
