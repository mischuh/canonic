name: artists
connection: chinook_pg
table: 'chinook."Artist"'
grain: [ArtistId]
description: "One row per recording artist or band."

columns:
  - { name: ArtistId, type: int,    nullable: false }
  - { name: Name,     type: string, nullable: true  }

dimensions:
  - { name: artist_name, column: Name }

meta:
  provenance: human_curated
